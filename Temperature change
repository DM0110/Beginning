const kelvin = 290;
let celsius = kelvin - 273;

// patter for fahrenheit tem: Celsius * (9/5) + 32
let fahrenheit = Celsius * (9/5) + 32
fahrenheit = Math.floor(Celsius * (9/5) + 32);
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit`);
